[TENANT ISOLATION REPORT.md](https://github.com/user-attachments/files/32386486/TENANT.ISOLATION.REPORT.md)
# NABBAH — تقرير طبقة عزل الشركات (Tenant Isolation Report)
**Phase 1.2** · توحيد وتقوية العزل + اختبارات · **بدون كسر أي وظيفة**

---

## ملخّص تنفيذي

**العزل الحالي قوي وسليم** — لا توجد ثغرة تسرّب بيانات بين الشركات. تم توحيد المنطق المكرّر في helpers مركزية، وبناء اختبارات شاملة تثبّت أن شركة A لا يمكنها الوصول لبيانات شركة B.

---

## PART 1: تدقيق نمط العزل الحالي

### كيف يعمل العزل اليوم
- **المصدر الموثوق:** `user.company_id` (من رمز JWT الموقّع) — لا يأتي من طلب العميل.
- **نقاط الفلترة:** 83 نقطة `company_id ==` عبر الاستعلامات.
- **التحقق من الملكية:** 42 نقطة `company.owner_id != user.id` للعمليات الحسّاسة.

### الأنماط المكتشفة
| النمط | الاستخدام | التقييم |
|---|---|---|
| `s.get(Company, user.company_id)` | 53 مرة | ✅ آمن — company_id من JWT |
| `company.owner_id != user.id` | 42 مرة | ✅ يمنع غير المالك |
| `check_permission(...)` | 8 مرات | ✅ RBAC |
| وصول عبر `branch_id` | 9 نقاط | ✅ كلها محميّة (انظر أدناه) |

---

## فحص ثغرة IDOR (تمرير معرّف شركة أخرى)

**الخطر المحتمل:** أن يمرّر مستخدم `branch_id` يخصّ شركة أخرى فيصل لبياناتها.

**النتيجة: لا ثغرة.** فُحصت كل نقاط `s.get(CompanyBranch, ...)` التسع:

| السطر | الحالة |
|---|---|
| 536, 2406, 2424, 2446, 2791, 4223 | ✅ محمي صراحة (`owner_id`/`company_id` check) |
| 2869, 3084 | ✅ آمن — `branch_id` مصدره `CompanyModuleEntry` مُقيَّد أصلاً بالشركة (استخدام للاسم فقط) |
| 4395 | ✅ محمي صراحة (`br.company_id != company.id`) |

**كل نقطة وصول للفرع تتحقق أن الفرع يخص شركة المستخدم.**

---

## PART 2: التوحيد (Helpers مركزية جديدة)

أُضيفت 4 دوال مركزية تُوحّد المنطق الموجود — **دون تغيير سلوكه**:

| الدالة | الوظيفة |
|---|---|
| `get_active_company(s, user)` | يُرجع شركة المستخدم بعد التحقق (موجودة + نشطة) |
| `require_company_owner(s, user)` | يُرجع الشركة بعد تأكيد الملكية (للعمليات الحسّاسة) |
| **`get_owned_branch(s, user, branch_id)`** | **يمنع IDOR** — يرفض فرعاً من شركة أخرى |
| `scoped_branches(s, company_id)` | يُرجع فروع الشركة النشطة فقط |

**ملاحظة منهجية:** الـhelpers **متاحة للتوحيد التدريجي**. لم نُعِد كتابة الـ53 endpoint (خطر كسر) — العزل الحالي يبقى شغّالاً، والـhelpers تُستخدم في التطوير القادم لتقليل التكرار.

---

## PART 3: الاختبارات

`test_tenant_isolation.py` — يثبّت العزل:

| الاختبار | يتحقق |
|---|---|
| `get_owned_branch_rejects_other_company` | **مستخدم A لا يصل لفرع B → 403** (منع IDOR) |
| `get_active_company_no_company` | مستخدم بلا شركة → 403 |
| `require_company_owner_rejects_non_owner` | عضو غير مالك → 403 |
| `scoped_branches_only_own` | كل شركة ترى فروعها فقط — لا تداخل |
| `http_company_a_cannot_see_b` | عبر HTTP: رمز A لا يكشف بيانات B |

### نتائج التحقق المباشر
```
① مستخدم A → فرعه:      مسموح ✅
② مستخدم A → فرع شركة B:  مرفوض 403 ✅ (منع IDOR)
③ فرع غير موجود:         404 ✅
```

**العزل يعمل: كل محاولة وصول لشركة أخرى مرفوضة.**

---

## ما لم يُلمَس (كما طُلب)
- ❌ لا تغيير في منطق الأعمال
- ❌ لا تغيير في الصيغ أو الحسابات
- ❌ لا تغيير في الواجهة
- ❌ لا migration
- ❌ لم تُعَد كتابة الـendpoints (تجنّب الكسر)

**التغيير الوحيد:** إضافة 4 helpers (لا تُستدعى قسراً — متاحة للتوحيد التدريجي).

---

## الخلاصة الأمنية

| البُعد | التقييم |
|---|---|
| عزل على مستوى الاستعلام | ✅ قوي (83 نقطة) |
| منع IDOR (تمرير معرّف آخر) | ✅ محمي بالكامل |
| مصدر company_id | ✅ من JWT الموقّع (لا من العميل) |
| العمليات الحسّاسة | ✅ تتطلب ملكية |
| توحيد الكود | ✅ helpers مركزية جاهزة |

**لا ثغرة تسرّب بيانات بين الشركات.**

---
*تقرير Phase 1.2 — عزل الشركات. لا وظيفة كُسرت.*
